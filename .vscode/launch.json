{
  "version": "0.2.0",
  "configurations": [    
    {
      "name": "Debug Expo (Android)",
      "type": "expo",
      "request": "attach",
      "projectRoot": "${workspaceFolder}",
      "bundlerHost": "127.0.0.1",
      "bundlerPort": "8081",
      "preLaunchTask": "Start Expo (Android)"
    },
    {
      "name": "Attach to Expo (app already running)",
      "type": "expo",
      "request": "attach",
      "projectRoot": "${workspaceFolder}",
      "bundlerHost": "127.0.0.1",
      "bundlerPort": "8081"
    }
  ]
}
